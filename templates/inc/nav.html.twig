<nav id="animNavBar">
    {% if app.user %}
        {% if app.user.administrateur %}
            <a href="{{ path('gerer_villes') }}" title="GererVilles"> | Gerer les Villes</a>
            <a href="{{ path('admin_dashboard_index') }}" title="Panneau d'administration"> | Administration</a>
        {% endif %}
        <a href="{{ path('sorties_ajouter') }}" title="AjoutSortie"> | Créer une sortie </a>
        <a href="{{ path('user_modifier', {'id': app.user.id}) }}" title="Modifier le profil de l'utilisateur"> | Modifier mon profil</a>
        <a href="{{ path('app_logout') }}" title="Logout"> | Se déconnecter <i class="fa-regular fa-user"></i> ({{ app.user.pseudo }})</a>
    {% endif %}
</nav>