<nav>
    {% if app.user %}
        {% if app.user.administrateur %}
            <a style="transform: scale(1.4) ; transition: 0.5s;" href="{{ path('admin_dashboard_index') }}" title="Panneau d'administration"> Administration</a>
            {#           <a href="{{ path('campus_gerer') }}" title="GererCampus"> Gerer les Campus</a> #}
        {% endif %}
        <a style="transform: scale(1.4) ; transition: 0.5s;" href="{{ path('sorties_ajouter') }}" title="AjoutSortie"> Créer une sortie </a>
        {#        <a href="{{ path('gerer_villes') }}" title="GererVilles"> Gerer les Villes</a> #}
        <a style="transform: scale(1.4) ; transition: 0.5s;" href="{{ path('user_modifier', {'id': app.user.id}) }}" title="Modifier le profil de l'utilisateur"> Modifier
            mon profil</a>
        <a style="transform: scale(1.4) ; transition: 0.5s;" href="{{ path('app_logout') }}" title="Logout"> Se déconnecter <img src="{{ asset('img/ninja.png') }}"
                                                                               alt="ninja"
                                                                               class="img-fluid ea-lightbox-thumbnail"
                                                                               width="20" height="20">
            ({{ app.user.pseudo }})</a>
    {% endif %}
</nav>
   