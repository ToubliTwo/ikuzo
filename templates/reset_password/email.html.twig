<h1>Bonjour ! </h1>

<p>Vous venez d'avouer avoir des troubles de la mémoire ; bonne nouvelle, nous pouvons vous aider. </p>

<p>Pour réinitialiser votre mot de passe, vous pouvez cliquer sur le lien suivant :</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>Dépêchez-vous, ce lien expirera 💣 dans 1 heure  ! </p> {# {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }} cette instruction indique le délai d'expiration sous la forme : "1 hour" #}

<p><small>Pour tout autres types de troubles, veuillez consulter un spécialiste.</small></p>

<p>A bientôt ! </p>
